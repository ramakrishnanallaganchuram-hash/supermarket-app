name: Deploy Supermarket App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Copy files to EC2
        uses: appleboy/scp-action@master
        with:
          host: "43.204.229.125"
          username: "ubuntu"
          key: ${{ secrets.EC2_KEY }}
          source: "app.py,templates/,requirements.txt"
          target: "/home/ubuntu/supermarket"

      - name: Deploy to EC2
        uses: appleboy/ssh-action@master
        with:
          host: "43.204.229.125"
          username: "ubuntu"
          key: ${{ secrets.EC2_KEY }}
          script: |
            sudo chown -R ubuntu:ubuntu /home/ubuntu/supermarket
            cd /home/ubuntu/supermarket
            source /home/ubuntu/supermarket-env/bin/activate
            pip install -r requirements.txt
            pkill -f "python3 app.py" || true
            sleep 1
            nohup /home/ubuntu/supermarket-env/bin/python3 /home/ubuntu/supermarket/app.py > /home/ubuntu/supermarket/app.log 2>&1 &
            disown
            echo "Done"
